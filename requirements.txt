# Optional dependencies for Genesis-Engine
# The initial prototype uses only the Python standard library.

# To evolve towards real embeddings and vector databases:
# langchain
# chromadb
sentence-transformers
numpy
einops          # required by nomic-embed-text-v1.5
pyyaml          # genesis.yaml config support
openai          # agent connector — DeepSeek, OpenAI, Gemini (OpenAI-compatible)
# faiss-cpu
